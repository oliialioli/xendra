# Copy this file to .env.local (gitignored) and fill in your Supabase
# project's own values -- Project Settings -> API in the Supabase dashboard.
# See docs/BOATS.md for the full setup (migration, RLS policies, etc.).
#
# Both missing/empty: the message-boats feature runs in local-only
# development mode instead (localStorage, never shared between visitors).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
